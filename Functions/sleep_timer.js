//sleepTimer
const sleepTimer = (alarm) => {
  console.log('My alarm is set for: ' + alarm);
}

sleepTimer('8:30AM');
/*
alarm is a parameter of sleepTimer. 8:30AM is an argument of sleepTimer.
Parameters capture the value of arguments passed to functions.